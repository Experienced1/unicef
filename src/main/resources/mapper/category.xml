<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unicef.repository.CategoryRepository">
    <insert id="save" parameterType="com.unicef.domain.Category">
        insert into
            category_mst
        values(
            0,
            #{category_list},
            now(),
            now()
        )
    </insert>
</mapper>